For mysql:
- npm install --save sequelize
- npm install --save mysql2

For postgres
- npm install --save sequelize
- npm install --save pg
- npm install --save postgres

Access to GCP MySQL DB
- sudo docker exec -it {container_id} bash
- mysql -u root -p  //root root
- show databases;

Access to GCP Postgres DB
sudo docker pull postgres:14.1-alpine
docker run --name postgres -e POSTGRES_PASSWORD=postgres -p 5432:5432 -d postgres

    get into container
    - sudo docker exec -it {container_id} bash

    Access postges
    - psql -U postgres
    
    list databases
    - \l

    Connect to database tododb
    - \c tododb  

    check tables
    - \d
    - TABLE todos;

    -\q  --> exit